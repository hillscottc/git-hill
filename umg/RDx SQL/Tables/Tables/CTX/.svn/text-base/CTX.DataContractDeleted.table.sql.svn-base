CREATE TABLE CTX.DataContractDeleted(
	ID bigint IDENTITY(1,1) NOT NULL,
	CONTRACT_ID numeric (38, 0) NOT NULL,
	CHANGE_DATE_TIME datetime default(getdate()) NOT NULL,
	WORKFLOW_CODE varchar(3) default('L') NOT NULL,
) ON [PRIMARY]